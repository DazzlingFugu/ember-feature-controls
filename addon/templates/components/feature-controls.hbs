<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    {{#each model as |flag|}}
      <tr>
        <td>{{flag.key}}</td>
        <td>{{flag.description}}</td>
        <td>{{input type='checkbox' checked=flag.isEnabled change=(action 'doToggleFeature' flag.key flag.isEnabled) data-test-checkbox-flag=flag.key}}</td>
      </tr>
    {{/each}}
  </tbody>
</table>

{{#if showRefresh}}
  <button {{action 'refresh'}} data-test-button-refresh>Refresh</button>
{{/if}}

{{#if showReset}}
  <button {{action 'reset'}} data-test-button-reset>Reset</button>
{{/if}}
